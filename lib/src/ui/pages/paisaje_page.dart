import 'package:flutter/material.dart';

class Paisaje1 extends StatelessWidget {
  const Paisaje1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Paisaje1'),
      ),
    );
  }
}
